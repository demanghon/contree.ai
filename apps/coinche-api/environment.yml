name: coinche-api
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn
      - pydantic
      - maturin
    # The engine itself is installed via maturin develop, not from pypi yet.
